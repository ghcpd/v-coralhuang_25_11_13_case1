You are an AI backend engineer and test engineer working inside a real execution environment with Python, pip, and a shell available. You will receive a single Python file containing a Flask application that uses the application-factory pattern, configuration classes (BaseConfig, DevConfig, TestConfig), Flask extensions such as SQLAlchemy, Flask-Migrate, Flask-Login, Flask-Mail, and Flask-Babel, a User model, several routes including “/”, “/protected”, and “/login”, CLI commands for translation (translate init, update, compile), asynchronous email functions (send_email and send_async_email), and a set of pytest-style test cases that include both reasonable failing cases and fundamentally incorrect ones. The file intentionally contains many defects, and the tests themselves also contain design issues. Your goal is to fix the application and tests without inventing results, build a reusable one-click test environment, and verify correctness through real executed output. The code contains issues including configuration and localization errors such as missing LANGUAGES, BABEL_DEFAULT_LOCALE, and BABEL_DEFAULT_TIMEZONE in BaseConfig; DevConfig defining i18n while TestConfig fails to inherit or redefine it, causing KeyError for current_app.config["LANGUAGES"] under testing and causing Babel locale selector to behave incorrectly; and create_app defaulting to DevConfig, masking configuration problems. It also contains context-related errors such as Babel get_locale() accessing request.accept_languages and LANGUAGES without a proper request context; CLI translate commands using current_app instead of the passed app, failing when no app_context is active during testing; and asynchronous email threads receiving the current_app proxy instead of the real app instance via _get_current_object(), making the background thread unable to use app_context. Test cases contain invalid usage such as calling get_locale() without a request context, creating a global app via create_app() using DevConfig instead of TestConfig which hides configuration issues, and making unreasonable assertions about translation output. The project structure is also non-reusable, because everything is inside a single file and there are no environment scripts such as requirements.txt, Dockerfile, setup.sh, run_test.sh, or run_test.bat, and no README explaining the background or how to run tests. Your task is to transform this single-file project into a clean, modular, reproducible, and testable project with all tests passing. You must execute tests in the real environment using pytest and must not fabricate outputs. All shown test results must come from actual execution. You must review and repair the entire application. You must ensure configuration classes include correct multilingual settings, ensure create_app consistently uses a unified configuration entry and accepts TestConfig during testing, fix Babel locale logic to correctly handle LANGUAGES and request.accept_languages, implement clear locale precedence between User.locale and Accept-Language headers, verify that Flask-Login’s login_view and login_message localization works correctly under TestConfig, fix asynchronous email logic by passing a real app instance via _get_current_object(), and correct CLI command registration so that app.test_cli_runner() works without manually pushing an app context. You may reorganize the project into modules such as config.py, app/init.py, app/models.py, app/email.py, app/cli.py, and tests/, as long as the result is coherent and testable. You must repair all existing tests, deleting or rewriting tests that use incorrect patterns and ensuring all remaining tests are logically aligned with expected behavior. You must add new tests covering scenarios such as Babel selecting “zh” under TestConfig when Accept-Language is zh; get_locale returning the user’s locale when the user is logged in and User.locale is set; login_required messages being properly localized for unauthenticated access; CLI translate commands running correctly through app.test_cli_runner() without an extra app_context; and asynchronous email sending not raising context-related errors, verified via monkeypatching mail.send. After fixing the code and tests, you must execute pytest and show the real output. If failures occur, you must show the actual traceback, analyze the cause, and fix the code or tests until all pass or provide justified reasons for any intentionally remaining failures. You must also deliver a fully reusable environment including requirements.txt listing all dependencies, a Dockerfile using an official Python image that installs dependencies and runs tests, setup.sh for Linux/macOS that detects the OS, creates and activates a virtual environment, and installs requirements, run_test.sh for Linux/macOS that activates the virtual environment and runs pytest, and run_test.bat for Windows performing equivalent steps. The project must run end-to-end in a clean environment simply by executing ./setup.sh and ./run_test.sh, or the Windows batch script. You must also deliver a README.md that clearly explains the original problems, the repair strategy, the module structure, and instructions for setup and testing, along with any known limitations or future improvements.